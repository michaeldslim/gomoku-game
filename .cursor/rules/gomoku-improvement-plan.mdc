---
description: Gomoku game improvement backlog — UI, AI, gameplay, and infra
alwaysApply: false
---

# Gomoku Improvement Plan

Last reviewed: 2026-09-07.

**Notation:** `P0` = **Phase 0**, `P1` = **Phase 1**, … `P5` = **Phase 5**.

## Progress summary

| Phase | Done | Total |
|-------|------|-------|
| Phase 0 (P0) | 5 | 5 |
| Phase 1 (P1) | 7 (+1 partial) | 8 |
| Phase 2 (P2) | 5 | 5 |
| Phase 3 (P3) | 4 | 4 |
| Phase 4 (P4) | — | *cancelled* |
| Phase 5 (P5) | 10 | 16 |
| **All** | **31** (+1 partial) | **38** *(P4 skipped)* |

### Design decisions (P5 · locked 2026-09-07)

| 항목 | 결정 |
|------|------|
| Career | **옵션 A** — 점수 런(0→100) + Career 승진 이중 트랙 |
| Theme | **밝은 톤** (`colors.ts`) |
| Avatars | 14종 — 범용 10 + 오목 전용 4 (`baduk-kid`, `stone-sage`, `panda-gomoku`, `focus-girl`) |

> APK·태블릿 QA(갤럭시 탭 우선): `.cursor/rules/gomoku-ios-ipa-build-plan.mdc` UI-0…3 참조.

### Extra (not numbered in original audit)

- [x] Master celebration fireworks / popup race fix (`Game.tsx`)
- [x] Dev test harness — `USE_TEST_MASTER_THRESHOLD` + README test section (`src/constants/scoring.ts`)
- [x] Leaderboard clear → game score resync on back (`App.tsx`, `useScoreProgression.ts`)
- [x] Easier AI tuning — intermediate pool 4/5 widened; expert easy relaxed (`aiLogic.ts`)

---

## Phase 0 (P0) — Bugs / Correctness

- [x] **#1 Leaderboard** — `getLatestRecord()` by `updatedAt`; `withoutRecord()` on updates (`leaderboard.ts`)
- [x] **#2 Gameplay** — Master celebration calls `onStartFreshRun(MASTER_SCORE_THRESHOLD)`; new run on threshold (`Game.tsx`, `scoring.ts`)
- [x] **#3 Gameplay** — Lock board size at game start / restart; rotation no longer changes `activeBoardSize` mid-play (`Game.tsx`, `gameLogic.ts`)
- [x] **#4 Defaults** — Align `bgMusicEnabled` default to `false` (`Game.tsx` matches `settings.ts`)
- [x] **#5 Docs** — Remove stale border comments in `gameLogic.ts` / `Board.tsx`

---

## Phase 1 (P1) — UI / UX

- [x] **#6 Home** — Language toggle (KO/EN) on intro screen (`App.tsx`)
- [x] **#7 Home** — App version footer on intro; test mode shows `TEST master @ N` (`App.tsx`)
- [~] **#8 Settings** — Show APP_VERSION + runtimeVersion at bottom *(skipped — home footer version sufficient)*
- [x] **#9 Leaderboard** — Nickname per run row; top-only card removed (`leaderboard.ts`, `LeaderboardScreen.tsx`)
- [x] **#10 Game** — Undo reverts full vs-AI turn (cancel pending AI, invalidate stale callbacks) (`Game.tsx`)
- [x] **#11 Game** — Timer expiry warning before random auto-move (`useGameTimer.ts`, `Game.tsx`, `i18n.ts`)
- [x] **#12 Game** — Split `Game.tsx` into hooks (`useGameSounds`, `useGameTimer`, `useScoreProgression`)
- [x] **#13 README** — Features / Project Structure rewrite (`README.md`)

---

## Phase 2 (P2) — AI Logic

- [x] **#14 Intermediate** — Force `BLOCK_WIN_SCORE` then `URGENT_DEFENSE_SCORE` (800k open-four) before top-pool random (`aiLogic.ts`)
- [x] **#15 Expert** — `countWinThreats` scans candidate cells only (not full 23×23 board) (`aiLogic.ts`)
- [x] **#16 Expert** — No jitter on fork tiers; deterministic pick when `score >= FORK_BLOCK_SCORE` (40M); easy mode relaxed (`aiLogic.ts`)
- [x] **#17 AI depth** — Depth-2 minimax on top-8 when Expert Hard (`EXPERT_TOP_POOL_HARD`) (`aiLogic.ts`)
- [x] **#18 Opening** — Opening book for game moves 1–3 (`getOpeningBookMove` in `aiLogic.ts`)

---

## Phase 3 (P3) — Gameplay / Progression

- [x] **#19 Scoring** — Show "+X" gain preview on win popup (`useScoreProgression.ts`, `VictoryPopup.tsx`, `Game.tsx`)
- [x] **#20 Expert switch** — Toast when auto-switching to Expert at 80 (`useScoreProgression.ts`, `Game.tsx`, `i18n.ts`)
- [x] **#21 2P mode** — Hide score banner when `vsAI === false` (`Game.tsx`)
- [x] **#22 Draw** — VictoryPopup styling + popup on draw (`VictoryPopup.tsx`, `useScoreProgression.ts`)

---

## Phase 4 (P4) — Infra / Release *(cancelled)*

- [~] **#23 OTA** — `expo-updates` check on startup + reload prompt *(cancelled)*
- [~] **#24 Versioning** — Extend `bump-runtime.js` to sync `package.json` *(cancelled)*
- [~] **#25 Tests** — Jest for `gameLogic`, `aiLogic`, leaderboard *(cancelled)*
- [~] **#26 CI** — `tsc --noEmit` / test job on PR *(cancelled)*

---

## Phase 5 (P5) — UI Redesign

*설계 확정 · 코드 미착수. 체크리스트 `#UI-n` = build plan과 동일.*

### P5-0 — Foundation

- [x] **#27 / UI-1** `src/constants/colors.ts` — bright theme tokens
- [x] **#28 / UI-2** `assets/avatars/` — 14 PNG; `src/constants/avatars.ts`
- [x] **#29 / UI-3** `PlayerAvatar.tsx` · `AvatarPicker.tsx`
- [x] **#30 / UI-4** `UserSettings` — `playerAvatarId`, `aiAvatarId`, `careerModeEnabled`

### P5-1 — Career · Avatars

- [x] **#31 / UI-5** `src/career/*` — rules, storage, provider
- [x] **#32 / UI-6** `PromotionOverlay.tsx` — bright gold banner; Career promotion
- [x] **#33 / UI-7** Career status screen
- [x] **#34 / UI-8** `SettingsScreen` — Career toggle, i18n

### P5-2 — Tablet landscape

- [x] **#35 / UI-9** `useScreenLayout.ts`
- [x] **#36 / UI-10** `PhoneLandscapeWarning.tsx` in `App.tsx`
- [x] **#37 / UI-11** `GameSideHud.tsx` (gomoku)
- [x] **#38 / UI-12** `LandscapeSideArt.tsx` — light wood + stone SVG
- [x] **#39 / UI-13** `Game.tsx` — 3-column wide layout

### P5-3 — Polish · QA

- [x] **#40 / UI-14** Home — `HomePlayerPreview.tsx` · Career badge, avatar preview
- [ ] **#41 / UI-15** **Galaxy Tab** + iPhone portrait regression (iPad optional)
- [ ] **#42 / UI-16** Play Store Galaxy Tab landscape screenshots

---

## Suggested next up

**P5 UI-15~16** Galaxy Tab QA + Play Store screenshots — next up. (`app.json` orientation + AndroidManifest 회전 잠금 해제 ✓)

## Files to touch (reference)

- `src/services/leaderboard.ts` — P0-1 ✓, P1-9 ✓
- `src/components/Game.tsx` — P0-2 ✓, P0-3/4 ✓, P1-10/11 ✓, P3-19/20/21 ✓
- `src/hooks/` — P1-11/12 ✓ (`useGameSounds`, `useGameTimer`, `useScoreProgression`); P3-19/20/22 ✓
- `src/utils/gameLogic.ts` — P0-3/5 ✓
- `src/utils/aiLogic.ts` — P2 ✓
- `App.tsx` — P1-6 ✓, P1-7 ✓
- `src/constants/scoring.ts` — master threshold + dev test ✓
- `src/utils/i18n.ts` — P3-20 ✓
- `src/components/VictoryPopup.tsx` — P3-19/22 ✓
- `README.md` — P1-13 ✓
- **P5 (planned):** `src/constants/colors.ts`, `src/constants/avatars.ts`, `assets/avatars/`, `src/career/`, `src/components/PlayerAvatar.tsx`, `PromotionOverlay.tsx`, `GameSideHud.tsx`, `LandscapeSideArt.tsx`, `src/hooks/useScreenLayout.ts`, `PhoneLandscapeWarning.tsx`, `App.tsx`, `Game.tsx`, `SettingsScreen.tsx`
