---
description: Gomoku game improvement backlog — UI, AI, gameplay, and infra
alwaysApply: false
---

# Gomoku Improvement Plan (v2.1.1 audit)

Last reviewed: 2026-07-10.

**Notation:** `P0` = **Phase 0**, `P1` = **Phase 1**, … `P4` = **Phase 4**.

## Progress summary

| Phase | Done | Total |
|-------|------|-------|
| Phase 0 (P0) | 5 | 5 |
| Phase 1 (P1) | 1 (+1 partial) | 8 |
| Phase 2 (P2) | 1 | 5 |
| Phase 3 (P3) | 0 | 4 |
| Phase 4 (P4) | 0 | 4 |
| **All** | **7** (+1 partial) | **26** |

### Extra (not numbered in original audit)

- [x] Master celebration fireworks / popup race fix (`Game.tsx`)
- [x] Dev test harness — `USE_TEST_MASTER_THRESHOLD` + README test section (`src/constants/scoring.ts`)

---

## Phase 0 (P0) — Bugs / Correctness

- [x] **#1 Leaderboard** — `getLatestRecord()` by `updatedAt`; `withoutRecord()` on updates (`leaderboard.ts`)
- [x] **#2 Gameplay** — Master celebration calls `onStartFreshRun(MASTER_SCORE_THRESHOLD)`; new run on threshold (`Game.tsx`, `scoring.ts`)
- [x] **#3 Gameplay** — Lock board size at game start / restart; rotation no longer changes `activeBoardSize` mid-play (`Game.tsx`, `gameLogic.ts`)
- [x] **#4 Defaults** — Align `bgMusicEnabled` default to `false` (`Game.tsx` matches `settings.ts`)
- [x] **#5 Docs** — Remove stale border comments in `gameLogic.ts` / `Board.tsx`

---

## Phase 1 (P1) — UI / UX

- [ ] **#6 Home** — Language toggle (KO/EN) on intro screen
- [x] **#7 Home** — App version footer on intro; test mode shows `TEST master @ N` (`App.tsx`)
- [ ] **#8 Settings** — Show APP_VERSION + runtimeVersion at bottom
- [ ] **#9 Leaderboard** — Nickname per run row (not only top card)
- [ ] **#10 Game** — Undo: revert AI reply too, or relabel button
- [ ] **#11 Game** — Timer expiry warning before random auto-move
- [ ] **#12 Game** — Split `Game.tsx` into hooks (`useGameSounds`, `useGameTimer`, `useScoreProgression`)
- [ ] **#13 README** — Full Features / Project Structure rewrite *(partial done: master-celebration dev test section)*

---

## Phase 2 (P2) — AI Logic

- [x] **#14 Intermediate** — Force `BLOCK_WIN_SCORE` then `URGENT_DEFENSE_SCORE` (800k open-four) before top-pool random (`aiLogic.ts`)
- [ ] **#15 Expert** — `countWinThreats` perf on 23×23 (cache or top-N only)
- [ ] **#16 Expert** — Remove random jitter when `score >= 40_000_000`
- [ ] **#17 AI depth** — Optional depth-2 minimax for Expert Hard
- [ ] **#18 Opening** — Opening book for moves 1–3

---

## Phase 3 (P3) — Gameplay / Progression

- [ ] **#19 Scoring** — Show "+X" gain preview on win popup
- [ ] **#20 Expert switch** — Toast when auto-switching to Expert at 80
- [ ] **#21 2P mode** — Hide score banner when `vsAI === false`
- [ ] **#22 Draw** — VictoryPopup styling for draws

---

## Phase 4 (P4) — Infra / Release

- [ ] **#23 OTA** — `expo-updates` check on startup + reload prompt
- [ ] **#24 Versioning** — Extend `bump-runtime.js` to sync `package.json`
- [ ] **#25 Tests** — Jest for `gameLogic`, `aiLogic`, leaderboard
- [ ] **#26 CI** — `tsc --noEmit` / test job on PR

---

## Suggested next up

1. **P1** #6, #8, #10 — UX quick wins
2. **P2** #16, #15 — AI reliability / perf
3. **P4** #23, #25 — OTA + tests

## Files to touch (reference)

- `src/services/leaderboard.ts` — P0-1 ✓
- `src/components/Game.tsx` — P0-2 ✓, P0-3/4 ✓, P1-10/11/12, P3-19/20/21
- `src/utils/gameLogic.ts` — P0-3/5 ✓
- `src/utils/aiLogic.ts` — P2-14 ✓, P2-15/16/17/18
- `App.tsx` — P1-7 ✓
- `src/constants/scoring.ts` — master threshold + dev test ✓
- `src/utils/i18n.ts` — new strings
- `README.md` — P1-13 (partial ✓)
